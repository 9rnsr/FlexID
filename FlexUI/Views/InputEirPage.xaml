<?xml version="1.0" encoding="utf-8" ?>
<Page
  x:Class="FlexID.Views.InputEirPage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:i="using:Microsoft.Xaml.Interactivity"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:v="using:FlexID.Views"
  xmlns:vb="using:FlexID.Views.Behaviors"
  xmlns:vm="using:FlexID.ViewModels"
  HorizontalAlignment="Stretch"
  VerticalAlignment="Stretch"
  d:DataContext="{d:DesignInstance vm:InputEirViewModel}"
  NavigationCacheMode="Required"
  mc:Ignorable="d">

  <Page.Resources>
    <!--
    <Style TargetType="TabItem">
      <Setter Property="Padding" Value="8,2" />
    </Style>
    <Style TargetType="Label">
      <Setter Property="Margin" Value="4" />
      <Setter Property="VerticalAlignment" Value="Center" />
    </Style>
    <Style TargetType="Button">
      <Setter Property="Padding" Value="8,3" />
      <Setter Property="VerticalAlignment" Value="Center" />
    </Style>
    <Style TargetType="CheckBox">
      <Setter Property="Margin" Value="32,4" />
      <Setter Property="VerticalAlignment" Value="Center" />
    </Style>
    <Style TargetType="RadioButton">
      <Setter Property="Margin" Value="32,4" />
      <Setter Property="VerticalAlignment" Value="Center" />
    </Style>
    <Style TargetType="TextBox">
      <Setter Property="Padding" Value="3" />
      <Setter Property="VerticalAlignment" Value="Center" />
    </Style>
    <Style TargetType="ComboBox">
      <Setter Property="Padding" Value="6,5,3,3" />
      <Setter Property="VerticalAlignment" Value="Center" />
    </Style>
    -->
  </Page.Resources>

  <Grid Margin="16" RowSpacing="24">
    <Grid.RowDefinitions>
      <RowDefinition />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <Button
      Grid.Row="2"
      Width="128"
      Height="40"
      Margin="0,0,24,0"
      HorizontalAlignment="Right"
      Command="{x:Bind ViewModel.RunCommand}"
      Content="Run" />
  </Grid>

  <!--
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto" SharedSizeGroup="L" />
      <ColumnDefinition />
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" SharedSizeGroup="R" />
      <RowDefinition Height="Auto" SharedSizeGroup="R" />
      <RowDefinition Height="Auto" SharedSizeGroup="R" />
      <RowDefinition Height="Auto" SharedSizeGroup="R" />
      <RowDefinition Height="Auto" SharedSizeGroup="R" />
      <RowDefinition Height="Auto" SharedSizeGroup="R" />
      <RowDefinition Height="Auto" SharedSizeGroup="R" />
      <RowDefinition Height="Auto" SharedSizeGroup="R" />
      <RowDefinition />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <Label
      Grid.Row="0"
      Grid.Column="0"
      Content="Output File Path" />
    <Grid Grid.Row="0" Grid.Column="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition />
        <ColumnDefinition Width="8" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <TextBox Text="{Binding OutputFilePath}">
        <i:Interaction.Behaviors>
          <vb:PathDropBehavior AllowDropPath="SingleAny" DropCommand="{Binding SelectOutputFilePathCommand}" />
        </i:Interaction.Behaviors>
      </TextBox>
      <Button
        Grid.Column="2"
        Command="{Binding SelectOutputFilePathCommand}"
        Content="Browse" />
    </Grid>

    <Label
      Grid.Row="1"
      Grid.Column="0"
      Content="Nuclide" />
    <ComboBox
      Grid.Row="1"
      Grid.Column="1"
      ItemsSource="{Binding Nuclides}"
      SelectedValue="{Binding SelectedNuclide}" />

    <Label
      Grid.Row="2"
      Grid.Column="0"
      Content="Application of &#xA;Progeny Nuclide" />
    <Grid
      Grid.Row="2"
      Grid.Column="1"
      HorizontalAlignment="Center"
      VerticalAlignment="Center">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <RadioButton
        Grid.Column="0"
        Content="Yes"
        IsChecked="{Binding CalcProgeny}"
        IsEnabled="{Binding HasProgeny}" />
      <RadioButton
        Grid.Column="1"
        Content="No"
        IsChecked="{Binding CalcProgeny, Converter={StaticResource Inverted}}" />
    </Grid>

    <Label
      Grid.Row="3"
      Grid.Column="0"
      Content="Input Title &#xA;(Route, Form, etc)" />
    <ComboBox
      Grid.Row="3"
      Grid.Column="1"
      DisplayMemberPath="Title"
      ItemsSource="{Binding Inputs}"
      SelectedValue="{Binding SelectedInput}" />

    <Label
      Grid.Row="4"
      Grid.Column="0"
      Content="Computational &#xA;Time Mesh" />
    <Grid Grid.Row="4" Grid.Column="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition />
        <ColumnDefinition Width="8" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <TextBox Text="{Binding ComputeTimeMeshFilePath}">
        <i:Interaction.Behaviors>
          <vb:PathDropBehavior AllowDropPath="SingleAny" DropCommand="{Binding SelectComputeTimeMeshFilePathCommand}" />
        </i:Interaction.Behaviors>
      </TextBox>
      <Button
        Grid.Column="2"
        Command="{Binding SelectComputeTimeMeshFilePathCommand}"
        Content="Browse" />
    </Grid>

    <Label
      Grid.Row="5"
      Grid.Column="0"
      Content="Output &#xA;Time Mesh" />
    <Grid Grid.Row="5" Grid.Column="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition />
        <ColumnDefinition Width="8" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <TextBox Text="{Binding OutputTimeMeshFilePath}">
        <i:Interaction.Behaviors>
          <vb:PathDropBehavior AllowDropPath="SingleAny" DropCommand="{Binding SelectOutputTimeMeshFilePathCommand}" />
        </i:Interaction.Behaviors>
      </TextBox>
      <Button
        Grid.Column="2"
        Command="{Binding SelectOutputTimeMeshFilePathCommand}"
        Content="Browse" />
    </Grid>

    <Label
      Grid.Row="6"
      Grid.Column="0"
      Content="Commitment Period" />
    <Grid
      Grid.Row="6"
      Grid.Column="1"
      HorizontalAlignment="Center"
      VerticalAlignment="Center">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <TextBox
        Grid.Column="0"
        Width="60"
        Margin="32,4"
        HorizontalContentAlignment="Center"
        d:Text="50"
        Text="{Binding CommitmentPeriod}" />
      <ComboBox
        Grid.Column="1"
        Width="120"
        Margin="32,4"
        ItemsSource="{Binding CommitmentPeriodUnits, Mode=OneTime}"
        SelectedValue="{Binding SelectedCommitmentPeriodUnit}" />
    </Grid>

    <Label
      Grid.Row="7"
      Grid.Column="0"
      Content="Intake Age" />
    <ComboBox
      Grid.Row="7"
      Grid.Column="1"
      Width="120"
      HorizontalAlignment="Left"
      ItemsSource="{Binding IntakeAges, Mode=OneTime}"
      SelectedValue="{Binding SelectedIntakeAge}" />
  </Grid>
  -->

</Page>
