<UserControl
  x:Class="FlexID.Views.ColorScaleBar"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:vc="using:FlexID.Views.Converters"
  mc:Ignorable="d">

  <UserControl.Resources>
    <vc:ContourMaxMinConverter x:Key="ContourMaxMin" />
  </UserControl.Resources>

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="114" MaxWidth="114" />
    </Grid.ColumnDefinitions>

    <TextBox
      Grid.Row="0"
      MaxWidth="114"
      HorizontalContentAlignment="Center"
      AccessKey="CU"
      FontSize="{x:Bind FontSize, Mode=OneWay}"
      Text="{x:Bind ContourMax, Mode=TwoWay, Converter={StaticResource ContourMaxMin}}" />

    <Rectangle
      Grid.Row="1"
      Width="30"
      Height="250"
      Margin="0,8">
      <Rectangle.Fill>
        <LinearGradientBrush>
          <GradientStop Offset="0.00" Color="#FF0000" />
          <GradientStop Offset="0.25" Color="#FFA500" />
          <GradientStop Offset="0.50" Color="#FFFF00" />
          <GradientStop Offset="0.75" Color="#00FF00" />
          <GradientStop Offset="1.00" Color="#0000FF" />
        </LinearGradientBrush>
      </Rectangle.Fill>
    </Rectangle>

    <TextBox
      Grid.Row="2"
      HorizontalContentAlignment="Center"
      AccessKey="CL"
      FontSize="{x:Bind FontSize, Mode=OneWay}"
      Text="{x:Bind ContourMin, Mode=TwoWay, Converter={StaticResource ContourMaxMin}}" />

    <TextBlock
      Grid.Row="3"
      HorizontalAlignment="Center"
      d:Content="[Sv/h]">
      <Run Text="[" /><Run Text="{x:Bind ContourUnit, Mode=OneWay}" /><Run Text="]" />
    </TextBlock>
  </Grid>

</UserControl>
