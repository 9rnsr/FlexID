<?xml version="1.0" encoding="utf-8" ?>
<winex:WindowEx
  x:Class="FlexID.Views.ProgressWindow"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:v="using:FlexID.Views"
  xmlns:vm="using:FlexID.ViewModels"
  xmlns:winex="using:WinUIEx"
  Title="{x:Bind ViewModel.Title}"
  Width="800"
  Height="500"
  mc:Ignorable="d">

  <winex:WindowEx.SystemBackdrop>
    <MicaBackdrop />
  </winex:WindowEx.SystemBackdrop>

  <Grid Margin="16" RowSpacing="8">
    <Grid.RowDefinitions>
      <RowDefinition />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <ListView ItemsSource="{x:Bind ViewModel.Targets}" SelectionMode="Extended">
      <ListView.ItemTemplate>
        <DataTemplate x:DataType="vm:ProgressTargetViewModel">
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="2*" />
              <ColumnDefinition Width="1*" MaxWidth="200" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{x:Bind Title}" />
            <ProgressBar
              Grid.Column="1"
              VerticalAlignment="Stretch"
              IsIndeterminate="{x:Bind InProgress, Mode=OneWay}" />
          </Grid>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>

    <Button
      Grid.Row="1"
      HorizontalAlignment="Right"
      Command="{x:Bind ViewModel.AbortOrCloseCommand}"
      Content="{x:Bind GetButtonText(ViewModel.InProgress), Mode=OneWay}" />
  </Grid>

</winex:WindowEx>
