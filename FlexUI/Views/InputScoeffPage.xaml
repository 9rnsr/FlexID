<?xml version="1.0" encoding="utf-8" ?>
<Page
  x:Class="FlexID.Views.InputScoeffPage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:i="using:Microsoft.Xaml.Interactivity"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:v="using:FlexID.Views"
  xmlns:vb="using:FlexID.Views.Behaviors"
  xmlns:vm="using:FlexID.ViewModels"
  HorizontalAlignment="Stretch"
  VerticalAlignment="Stretch"
  d:DataContext="{d:DesignInstance vm:InputScoeffViewModel}"
  NavigationCacheMode="Required"
  mc:Ignorable="d">

  <Page.Resources>
    <!--
    <Style TargetType="TabItem">
      <Setter Property="Padding" Value="8,2" />
    </Style>
    <Style TargetType="Label">
      <Setter Property="Margin" Value="4" />
      <Setter Property="VerticalAlignment" Value="Center" />
    </Style>
    <Style TargetType="Button">
      <Setter Property="Padding" Value="8,3" />
      <Setter Property="VerticalAlignment" Value="Center" />
    </Style>
    <Style TargetType="CheckBox">
      <Setter Property="Margin" Value="32,4" />
      <Setter Property="VerticalAlignment" Value="Center" />
    </Style>
    <Style TargetType="RadioButton">
      <Setter Property="Margin" Value="32,4" />
      <Setter Property="VerticalAlignment" Value="Center" />
    </Style>
    <Style TargetType="TextBox">
      <Setter Property="Padding" Value="3" />
      <Setter Property="VerticalAlignment" Value="Center" />
    </Style>
    <Style TargetType="ComboBox">
      <Setter Property="Padding" Value="6,5,3,3" />
      <Setter Property="VerticalAlignment" Value="Center" />
    </Style>
    -->
  </Page.Resources>

  <Grid>
    <Grid Margin="24" RowSpacing="24">
      <Grid.RowDefinitions>
        <RowDefinition />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>

      <Button
        Grid.Row="2"
        Width="128"
        Height="40"
        Margin="0,8,24,0"
        HorizontalAlignment="Right"
        Command="{x:Bind ViewModel.RunCommand}"
        Content="Run" />
    </Grid>
  </Grid>

  <!--
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto" SharedSizeGroup="L" />
      <ColumnDefinition />
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" SharedSizeGroup="R" />
      <RowDefinition Height="Auto" SharedSizeGroup="R" />
      <RowDefinition Height="Auto" SharedSizeGroup="R" />
      <RowDefinition Height="Auto" SharedSizeGroup="R" />
      <RowDefinition Height="Auto" SharedSizeGroup="R" />
      <RowDefinition />
      <RowDefinition Height="12" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <Label
      Grid.Row="0"
      Grid.Column="0"
      Content="Output File Path" />
    <Grid Grid.Row="0" Grid.Column="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition />
        <ColumnDefinition Width="8" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <TextBox Text="{Binding OutputFilePath}">
        <i:Interaction.Behaviors>
          <vb:PathDropBehavior />
        </i:Interaction.Behaviors>
      </TextBox>
      <Button
        Grid.Column="2"
        Command="{Binding SelectOutputFilePathCommand}"
        Content="Browse" />
    </Grid>

    <Label
      Grid.Row="1"
      Grid.Column="0"
      Content="Sex" />
    <UniformGrid
      Grid.Row="1"
      Grid.Column="1"
      HorizontalAlignment="Center"
      Columns="2">
      <CheckBox
        Grid.Column="0"
        Content="Male"
        IsChecked="{Binding CalcMale}" />
      <CheckBox
        Grid.Column="1"
        Content="Female"
        IsChecked="{Binding CalcFemale}" />
    </UniformGrid>

    <Label
      Grid.Row="2"
      Grid.Column="0"
      Content="SAF Interpolation" />
    <UniformGrid
      Grid.Row="2"
      Grid.Column="1"
      HorizontalAlignment="Center"
      Columns="2">
      <RadioButton
        Grid.Column="0"
        Content="PCHIP"
        IsChecked="{Binding CalcPchip}" />
      <RadioButton
        Grid.Column="1"
        Content="Linear"
        IsChecked="{Binding CalcPchip, Converter={StaticResource Inverted}}" />
    </UniformGrid>

    <Label
      Grid.Row="3"
      Grid.Column="0"
      Content="Output Format" />
    <Grid
      Grid.Row="3"
      Grid.Column="1"
      HorizontalAlignment="Center">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <RadioButton
        Grid.Column="0"
        Margin="16,4"
        Content="FlexID (*.txt)"
        IsChecked="{Binding IdacDoseCompatible, Converter={StaticResource Inverted}}" />
      <RadioButton
        Grid.Column="1"
        Margin="16,4"
        Content="IDAC-Dose 2.1 compatible (*.csv)"
        IsChecked="{Binding IdacDoseCompatible}" />
    </Grid>

    <Label
      Grid.Row="4"
      Grid.Column="0"
      Content="Target Nuclides" />
    <ListView
      Grid.Row="4"
      Grid.RowSpan="2"
      Grid.Column="1"
      MinHeight="64"
      ItemsSource="{Binding Nuclides}"
      ScrollViewer.CanContentScroll="True"
      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
      SelectionMode="Extended">

      <i:Interaction.Behaviors>
        <vb:NuclideSelectorBehavior />
      </i:Interaction.Behaviors>

      <ListView.ItemsPanel>
        <ItemsPanelTemplate>
          <vwp:VirtualizingWrapPanel />
        </ItemsPanelTemplate>
      </ListView.ItemsPanel>

      <ListView.ItemTemplate>
        <DataTemplate DataType="{x:Type vm:NuclideItem}">
          <StackPanel Width="100" Orientation="Horizontal">
            <CheckBox
              Margin="0"
              VerticalAlignment="Center"
              Focusable="False"
              IsChecked="{Binding IsChecked}" />
            <TextBlock
              Margin="4,0,0,0"
              VerticalAlignment="Center"
              Text="{Binding Nuclide}" />
          </StackPanel>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </Grid>
  -->

</Page>
