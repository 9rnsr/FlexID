<?xml version="1.0" encoding="utf-8" ?>
<winex:WindowEx
  x:Class="FlexID.Views.MainWindow"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:v="using:FlexID.Views"
  xmlns:vm="using:FlexID.ViewModels"
  xmlns:winex="using:WinUIEx"
  Title="{x:Bind ViewModel.Title}"
  Width="800"
  Height="900"
  mc:Ignorable="d">

  <Window.SystemBackdrop>
    <MicaBackdrop />
  </Window.SystemBackdrop>

  <NavigationView
    x:Name="NavigationView"
    IsBackButtonVisible="Collapsed"
    IsSettingsVisible="False"
    PaneDisplayMode="Top"
    PreviewKeyDown="NavigationView_PreviewKeyDown"
    SelectionChanged="NavigationView_SelectionChanged">

    <NavigationView.MenuItems>
      <NavigationViewItem
        AccessKey="1"
        Content="OIR"
        Tag="{x:Bind InputOirPageType}" />
      <NavigationViewItem
        AccessKey="2"
        Content="EIR"
        Tag="{x:Bind InputEirPageType}" />
      <NavigationViewItem
        AccessKey="3"
        Content="OIR S-Coeff"
        Tag="{x:Bind InputScoeffPageType}" />
    </NavigationView.MenuItems>

    <Frame x:Name="NavigationFrame" Navigated="NavigationFrame_Navigated" />
  </NavigationView>

</winex:WindowEx>
